<Page
    x:Class="Station.Views.DevicesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Station.ViewModels"
    Background="{StaticResource BackgroundSecondaryBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section with Stats and Filters -->
        <Grid Grid.Row="0"
              Background="{StaticResource BackgroundSecondaryBrush}"
              Padding="24,16"
              BorderBrush="{StaticResource BorderLightBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title and Stats Row -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <TextBlock Grid.Column="0"
                           Text="Quản lý Thiết bị"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center"/>

                <!-- Statistics Cards -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            Spacing="12">
                    <!-- Total Devices -->
                    <Border Background="{StaticResource BackgroundPrimaryBrush}"

                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16,12"
                            MinWidth="140">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Tổng thiết bị"
                                       FontSize="12"
                                       Foreground="{StaticResource PrimaryDarkBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.TotalDevices, Mode=OneWay}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource PrimaryDarkBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Online Devices -->
                    <Border Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="8"
                            Padding="16,12"
                            MinWidth="140">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Hoạt động"
                                       FontSize="12"
                                       Foreground="{StaticResource SeverityLowBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.OnlineDevices, Mode=OneWay}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource SeverityLowBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Offline Devices -->
                    <Border Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="8"
                            Padding="16,12"
                            MinWidth="140">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Ngoại tuyến"
                                       FontSize="12"
                                       Foreground="{StaticResource TextTertiaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.OfflineDevices, Mode=OneWay}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextTertiaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Fault Devices -->
                    <Border Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="8"
                            Padding="16,12"
                            MinWidth="140">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Lỗi"
                                       FontSize="12"
                                       Foreground="{StaticResource SeverityCriticalBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.FaultDevices, Mode=OneWay}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource SeverityCriticalBrush}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Filters Row -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Border Grid.Column="0"
                        Background="{StaticResource BackgroundPrimaryBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12,0"
                        Height="36"
                        MaxWidth="400"
                        HorizontalAlignment="Left">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <FontIcon Grid.Column="0"
                                  Glyph="&#xE721;"
                                  FontSize="14"
                                  Foreground="{StaticResource TextTertiaryBrush}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"/>

                        <TextBox Grid.Column="1"
                                 Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="Tìm kiếm theo tuyến, node..."
                                 BorderThickness="0"
                                 Background="Transparent"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 Padding="0"/>
                    </Grid>
                </Border>

                <!-- Filter Buttons -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="12">
                    <!-- Line Filter -->
                    <Button MinWidth="160"
                            Style="{StaticResource FilterDropdownButtonStyle}"
                            x:Name="LineFilterButton">
                        <Button.Flyout>
                            <Flyout Placement="Bottom"
                                    FlyoutPresenterStyle="{StaticResource DarkFlyoutPresenterStyle}"
                                    x:Name="LineFlyout">
                                <ListView ItemsSource="{x:Bind ViewModel.LineFilters, Mode=OneWay}"
                                          SelectedItem="{x:Bind ViewModel.SelectedLine, Mode=TwoWay}"
                                          Style="{StaticResource FlyoutListViewStyle}"
                                          MinWidth="200"
                                          ItemClick="FilterListView_ItemClick">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem" BasedOn="{StaticResource FlyoutListViewItemStyle}"/>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <TextBlock Text="{x:Bind}"
                                                       FontSize="13"
                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Flyout>
                        </Button.Flyout>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0"
                                      Glyph="&#xE81D;"
                                      FontSize="14"
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Bind ViewModel.SelectedLine, Mode=OneWay}"
                                       FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="8,0"/>
                            <FontIcon Grid.Column="2"
                                      Glyph="&#xE70D;"
                                      FontSize="10"
                                      Foreground="{StaticResource TextTertiaryBrush}"/>
                        </Grid>
                    </Button>

                    <!-- Status Filter -->
                    <Button MinWidth="160"
                            Style="{StaticResource FilterDropdownButtonStyle}"
                            x:Name="StatusFilterButton">
                        <Button.Flyout>
                            <Flyout Placement="Bottom"
                                    FlyoutPresenterStyle="{StaticResource DarkFlyoutPresenterStyle}"
                                    x:Name="StatusFlyout">
                                <ListView ItemsSource="{x:Bind ViewModel.StatusFilters, Mode=OneWay}"
                                          SelectedItem="{x:Bind ViewModel.SelectedStatus, Mode=TwoWay}"
                                          Style="{StaticResource FlyoutListViewStyle}"
                                          MinWidth="200"
                                          ItemClick="FilterListView_ItemClick">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem" BasedOn="{StaticResource FlyoutListViewItemStyle}"/>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <TextBlock Text="{x:Bind}"
                                                       FontSize="13"
                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Flyout>
                        </Button.Flyout>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0"
                                      Glyph="&#xE8F8;"
                                      FontSize="14"
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Bind ViewModel.SelectedStatus, Mode=OneWay}"
                                       FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="8,0"/>
                            <FontIcon Grid.Column="2"
                                      Glyph="&#xE70D;"
                                      FontSize="10"
                                      Foreground="{StaticResource TextTertiaryBrush}"/>
                        </Grid>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Devices List by Line/Node -->
        <Border Grid.Row="1"
                Margin="24"
                Background="{StaticResource BackgroundSecondaryBrush}"
                CornerRadius="{StaticResource CornerRadiusLarge}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                <ItemsRepeater ItemsSource="{x:Bind ViewModel.FilteredNodes, Mode=OneWay}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="16"/>
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <!-- Node Card -->
                            <Border Background="{StaticResource BackgroundPrimaryBrush}"
                                    BorderBrush="{StaticResource BorderLightBrush}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Node Header -->
                                    <Grid Grid.Row="0"
                                          Background="{StaticResource BackgroundAccentBrush}"
                                          Padding="20,16"
                                          CornerRadius="12,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Node Icon -->
                                        <Border Grid.Column="0"
                                                Background="{StaticResource BackgroundPrimaryBrush}"
                                                CornerRadius="8"
                                                Width="48"
                                                Height="48"
                                                Margin="0,0,16,0">
                                            <FontIcon Glyph="&#xE968;"
                                                      FontSize="24"
                                                      Foreground="{StaticResource PrimaryDarkBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Node Info -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                            <TextBlock Text="{Binding NodeName}"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <TextBlock FontSize="13" Foreground="{StaticResource TextSecondaryBrush}">
                                                    <Run Text="Tuyến:"/>
                                                    <Run Text="{Binding LineName}" FontWeight="SemiBold"/>
                                                </TextBlock>
                                                <TextBlock FontSize="13" Foreground="{StaticResource TextSecondaryBrush}">
                                                    <Run Text="•"/>
                                                    <Run Text="{Binding Location}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Node Status Badge -->
                                        <Border Grid.Column="2"
                                                Background="{Binding StatusBackgroundColor}"
                                                CornerRadius="8"
                                                Padding="12,8"
                                                VerticalAlignment="Center"
                                                Margin="0,0,12,0">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}"/>
                                                <TextBlock Text="{Binding StatusText}"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           Foreground="{Binding StatusColor}"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Expand/Collapse Button -->
                                        <Button Grid.Column="3"
                                                Click="NodeExpandButton_Click"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="8"
                                                VerticalAlignment="Center">
                                            <FontIcon x:Name="ExpandIcon"
                                                      Glyph="&#xE70D;"
                                                      FontSize="14"
                                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </Button>
                                    </Grid>

                                    <!-- Sensors List (Collapsible) -->
                                    <StackPanel Grid.Row="1"
                                                x:Name="SensorsPanel"
                                                Padding="20"
                                                Spacing="12"
                                                Visibility="{Binding IsExpanded}">
                                        
                                        <!-- Sensors Header -->
                                        <Grid Margin="0,0,0,8">
                                            <TextBlock Text="Cảm biến trong node"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock Text="{Binding SensorCountText}"
                                                       FontSize="13"
                                                       Foreground="{StaticResource TextTertiaryBrush}"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"/>
                                        </Grid>

                                        <!-- Sensors Grid -->
                                        <ItemsRepeater ItemsSource="{Binding Sensors}">
                                            <ItemsRepeater.Layout>
                                                <UniformGridLayout MinItemWidth="300"
                                                                   MinItemHeight="120"
                                                                   ItemsStretch="Fill"
                                                                   MinColumnSpacing="12"
                                                                   MinRowSpacing="12"/>
                                            </ItemsRepeater.Layout>
                                            <ItemsRepeater.ItemTemplate>
                                                <DataTemplate>
                                                    <!-- Sensor Card -->
                                                    <Border Background="{StaticResource BackgroundSecondaryBrush}"
                                                            BorderBrush="{StaticResource BorderLightBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="8"
                                                            Padding="16"
                                                            PointerEntered="SensorCard_PointerEntered"
                                                            PointerExited="SensorCard_PointerExited"
                                                            Tapped="SensorCard_Tapped">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="*"/>
                                                            </Grid.RowDefinitions>

                                                            <!-- Sensor Header -->
                                                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>

                                                                <Border Grid.Column="0"
                                                                        Background="{StaticResource PrimaryLightBrush}"
                                                                        CornerRadius="6"
                                                                        Width="32"
                                                                        Height="32"
                                                                        Margin="0,0,10,0">
                                                                    <FontIcon Glyph="{Binding TypeIcon}"
                                                                              FontSize="16"
                                                                              Foreground="{StaticResource PrimaryDarkBrush}"/>
                                                                </Border>

                                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                    <TextBlock Text="{Binding SensorName}"
                                                                               FontSize="14"
                                                                               FontWeight="SemiBold"
                                                                               Foreground="{StaticResource TextPrimaryBrush}"/>
                                                                    <TextBlock Text="{Binding SensorType}"
                                                                               FontSize="11"
                                                                               Foreground="{StaticResource TextTertiaryBrush}"/>
                                                                </StackPanel>

                                                                <!-- Sensor Status -->
                                                                <Ellipse Grid.Column="2"
                                                                         Width="10"
                                                                         Height="10"
                                                                         Fill="{Binding SensorStatusColor}"
                                                                         VerticalAlignment="Center"/>
                                                            </Grid>

                                                            <!-- Current Reading -->
                                                            <Border Grid.Row="1"
                                                                    Background="{StaticResource BackgroundAccentBrush}"
                                                                    CornerRadius="6"
                                                                    Padding="12,8"
                                                                    Margin="0,0,0,8">
                                                                <Grid>
                                                                    <TextBlock Text="{Binding CurrentValue}"
                                                                               FontSize="20"
                                                                               FontWeight="Bold"
                                                                               Foreground="{StaticResource PrimaryDarkBrush}"
                                                                               HorizontalAlignment="Left"
                                                                               VerticalAlignment="Center"/>
                                                                    <TextBlock Text="{Binding Unit}"
                                                                               FontSize="12"
                                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                                               HorizontalAlignment="Right"
                                                                               VerticalAlignment="Center"/>
                                                                </Grid>
                                                            </Border>

                                                            <!-- Sensor Details -->
                                                            <StackPanel Grid.Row="2" Spacing="4">
                                                                <TextBlock FontSize="11" Foreground="{StaticResource TextTertiaryBrush}">
                                                                    <Run Text="Cập nhật:"/>
                                                                    <Run Text="{Binding LastUpdateText}"/>
                                                                </TextBlock>
                                                                <TextBlock FontSize="11" Foreground="{StaticResource TextTertiaryBrush}">
                                                                    <Run Text="ID:"/>
                                                                    <Run Text="{Binding SensorId}"/>
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsRepeater.ItemTemplate>
                                        </ItemsRepeater>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>